
# Root Direction
ESYSIM_ROOT := /home/eddie/ESYSim

# python 3
PY3_DIR := /usr/include/python3.6

# Linux Command
MAKE   := make -s
AR     := ar qcv
AROPT  :=
RANLIB := ranlib
RM     := rm -vf
RMDIR  := rm -rvf
MKDIR  := mkdir -p
LN     := ln -s
LNDIR  := ln -s
DIFF   := diff
CS     := ;
MV     := mv -vf
EXEC   := ./

# File system
OEXT := o
DEXT := d
LEXT := a
EEXT :=
X    := /

# dependence, object, binary folder
BUILD_DIR := $(ESYSIM_ROOT)$(X)build
BIN_DIR := $(ESYSIM_ROOT)$(X)bin

# Include 
INC_DIR := -I$(ESYSIM_ROOT)$(X)libs
LINK_DIR :=
LINK_OBJ := $(BUILD_DIR)$(X)libs$(X)tinyxml$(X)*.$(OEXT) $(BUILD_DIR)$(X)libs$(X)netcfg$(X)*.$(OEXT) $(BUILD_DIR)$(X)libs$(X)*.$(OEXT)

# GNU C/C++ Command
CCPP   := g++
CC     := gcc
OFLAGS := -g -Wall $(LINK_DIR) $(LINK_OBJ)
CFLAGS := -g -Wall $(INC_DIR)

# SWIG command
SWIG   := swig -Wall -c++ -python -py3

